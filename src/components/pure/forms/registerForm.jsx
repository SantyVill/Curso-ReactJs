import React from 'react';

const RegisterForm = () => {

    const initialData = [
        {
            user:"",
            name:"",
            email:"",
            password:""
        }
    ]

    const [data, setData] = useState(initialState);

    return (
        <div>
            
        </div>
    );
}

export default RegisterForm;
